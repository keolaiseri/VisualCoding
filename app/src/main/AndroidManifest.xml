<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VisualCoding"
        tools:targetApi="31">

        <meta-data android:name="com.google.android.gms.phenotype.com.keola.visualcoding" android:value="com.keola.visualcoding" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".fireworks.FireworksActivity"
            android:exported="true"
            android:label="Fireworks Activity">
            <!-- Add any intent filters or other activity attributes if needed -->
        </activity>
        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".fireworks.FireworksSize"
            android:exported="true"
            android:label="Fireworks Size Activity">
            <!-- Add any intent filters or other activity attributes if needed -->
        </activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".fireworks.FireworksFinishedActivity"
            android:exported="true"
            android:label="Fireworks Finished Activity">
            <!-- Add any intent filters or other activity attributes if needed -->
        </activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".HomeScreenActivity"
            android:exported="true"
            android:label="Home Screen Activity">
            <!-- Add any intent filters or other activity attributes if needed --></activity>


        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".DetailActivity"
            android:exported="true"
            android:label="Detail Screen Activity">
            <!-- Add any intent filters or other activity attributes if needed --></activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".tunnel.TunnelArtActivity"
            android:exported="true"
            android:label="Tunnel Art Screen Activity">
            <!-- Add any intent filters or other activity attributes if needed --></activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".spiral.SpiralActivity"
            android:exported="true"
            android:label="Spiral Activity">
            <!-- Add any intent filters or other activity attributes if needed -->
        </activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".fractal.FractalActivity"
            android:exported="true"
            android:label="Fractal Activity">
            <!-- Add any intent filters or other activity attributes if needed -->
        </activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".writing.WritingActivity"
            android:exported="true"
            android:label="Writing Activity">
            <!-- Add any intent filters or other activity attributes if needed -->
        </activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".rain.RainActivity"
            android:exported="true"
            android:label="Writing Activity">
            <!-- Add any intent filters or other activity attributes if needed -->
        </activity>

        <activity
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:name=".draw.DrawingActivity"
            android:exported="true"
            android:label="Drawing Activity">
            <!-- Add any intent filters or other activity attributes if needed -->
        </activity>


    </application>

</manifest>